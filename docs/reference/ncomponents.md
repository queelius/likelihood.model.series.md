# Number of components in a series system model

Returns the number of components m in the series system. If the model
was constructed without parameter values, returns NULL.

## Usage

``` r
ncomponents(model, ...)
```

## Arguments

- model:

  a likelihood model object

- ...:

  additional arguments (currently ignored)

## Value

integer number of components, or NULL if not determinable
